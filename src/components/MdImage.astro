---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";

export interface Props {
  src: ImageMetadata | string;
  alt: string;
  width?: number;
  height?: number;
  class?: string;
}

let { src, alt, width = 800, height = 0, ...rest} = Astro.props as Props;
---

<Image
  src={src as unknown as string}
  alt={alt}
  format="avif"
  width={width}
  height={height}
  class={Astro.props.class ? Astro.props.class : "image"}
  {...rest}
/>

<style>
  .image {
    width: auto;
    max-width: 100%;
    height: auto;
    max-height: 80dvh;
    margin-left: auto;
    margin-right: auto;
    display:block;
  }
</style>
